import React from 'react';
import JSON from '../data.json';

const Player_Data = () => {

  return (
    <div>
      {JSON.map((key, val) => (
        <div>
        </div>
      ))}
    </div>
  );
}

export default Player_Data;
